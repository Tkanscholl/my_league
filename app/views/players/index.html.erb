<h1>Players</h1>

<div id="players">
  <% @players.each do |player| %>
    <%= render player %>
    <p>
      <%= link_to "Show this player", player %>
      <%= link_to 'Delete this Player', players_path(players), method: :delete%>
    </p>
  <% end %>
</div>

<%= link_to "New player", new_player_path %>
